import type { Metadata } from "next";import Link from "next/link";import { articles,learningPaths } from "@/lib/content";
export const metadata:Metadata={title:"Learning paths"};
export default function Paths(){return <main id="main-content" className="directory-page"><header className="directory-header"><p className="kicker">Curated sequences</p><h1>Learning paths</h1><p>Build understanding in a deliberate order. Each path begins with foundations and adds nuance one article at a time.</p></header><div className="path-grid">{learningPaths.map((p,i)=>{const time=p.slugs.reduce((n,s)=>n+(articles.find(a=>a.slug===s)?.minutes||0),0);return <Link className="path-card" href={`/learning-paths/${p.slug}`} key={p.slug}><span>Path {String(i+1).padStart(2,"0")}</span><h2>{p.title}</h2><p>{p.goal}</p><footer><span>{p.slugs.length} readings</span><span>{time} min</span><b>→</b></footer></Link>})}</div></main>}
