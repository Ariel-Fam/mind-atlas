import type { MetadataRoute } from "next";import { articlePath,articles,learningPaths,categories } from "@/lib/content";export const dynamic="force-static";const base=process.env.NEXT_PUBLIC_SITE_URL??"http://localhost:3000";const slug=(s:string)=>s.toLowerCase().replace(/&/g,"and").replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"");export default function sitemap():MetadataRoute.Sitemap{return ["","/explore","/effects","/learning-paths","/saved","/glossary","/references","/about"].map(url=>({url:base+url,lastModified:new Date("2026-09-08")})).concat(articles.map(a=>({url:base+articlePath(a),lastModified:new Date(a.updated)})),learningPaths.map(p=>({url:`${base}/learning-paths/${p.slug}`,lastModified:new Date("2026-09-08")})),categories.map(c=>({url:`${base}/topics/${slug(c)}`,lastModified:new Date("2026-09-08")})))}
